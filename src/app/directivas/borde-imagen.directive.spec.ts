import { BordeImagenDirective } from './borde-imagen.directive';

describe('BordeImagenDirective', () => {
  it('should create an instance', () => {
    const directive = new BordeImagenDirective();
    expect(directive).toBeTruthy();
  });
});
