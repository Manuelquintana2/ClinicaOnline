import { AppCardEstiloDirective } from './app-card-estilo.directive';

describe('AppCardEstiloDirective', () => {
  it('should create an instance', () => {
    const directive = new AppCardEstiloDirective();
    expect(directive).toBeTruthy();
  });
});
