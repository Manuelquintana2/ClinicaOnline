import { ValidacionInputDirective } from './validacion-input.directive';

describe('ValidacionInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidacionInputDirective();
    expect(directive).toBeTruthy();
  });
});
